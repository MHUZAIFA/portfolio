"use client";

// This component is a placeholder to fix build errors
// It appears to be referenced in the TypeScript cache but not actually used
export function GitNetworkGraph() {
  return null;
}
